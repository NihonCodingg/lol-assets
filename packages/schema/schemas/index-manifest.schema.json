{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/NihonCodingg/lol-assets/schemas/index-manifest.schema.json",
  "title": "Manifesto do índice",
  "description": "Único arquivo com nome fixo em todo o bucket (manifest.json). É o ponto de entrada: diz quais versões existem, qual é a atual, onde está o catálogo de navegação e busca de cada uma, quais fatias de índice ela tem e quais zips já estão pré-gerados. Tudo o mais é endereçado por hash.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "generatedAt",
    "currentVersion",
    "versions"
  ],
  "properties": {
    "schemaVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "generatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "checkedAt": {
      "description": "Última vez que a indexação automática conferiu que este índice ainda é o do patch atual (ADR 0018). Uma execução sem patch novo carimba o campo no máximo uma vez por dia. Ausente significa o mesmo que generatedAt — é o caso de todo manifesto recém-gerado e dos anteriores ao 1.3.0.",
      "type": "string",
      "format": "date-time"
    },
    "assetsBaseUrl": {
      "description": "Prefixo público do bucket. O front monta URL como `${assetsBaseUrl}/${storageKey}`.",
      "type": "string",
      "format": "uri"
    },
    "currentVersion": {
      "description": "Patch exibido por padrão. É o único com assets copiados (ADR 0007).",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "generation": {
      "description": "Assinatura de geração (T-38): o que o indexador produziria diferente hoje, mesmo com a mesma versão do jogo. Ausente nos índices gerados antes do T-38, e ausente significa reindexar.",
      "$ref": "#/$defs/generation"
    },
    "versions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/version"
      }
    }
  },
  "$defs": {
    "version": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "gameVersion",
        "indexedAt",
        "assetsCopied",
        "catalog",
        "shards"
      ],
      "properties": {
        "gameVersion": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        },
        "indexedAt": {
          "type": "string",
          "format": "date-time"
        },
        "assetsCopied": {
          "description": "true = os bytes estão no bucket. false = só índice; o front usa sourceUrl do ddragon.",
          "type": "boolean"
        },
        "catalog": {
          "description": "A projeção de navegação e busca desta versão (ADR 0010). É o primeiro documento pesado que o front carrega; as fatias de asset vêm depois, sob demanda.",
          "$ref": "#/$defs/catalogRef"
        },
        "totalAssets": {
          "type": "integer",
          "minimum": 0
        },
        "totalBytes": {
          "type": "integer",
          "minimum": 0
        },
        "shards": {
          "description": "Uma entrada por categoria presente nesta versão.",
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/shard"
          }
        },
        "zips": {
          "description": "Zips por categoria pré-gerados pelo indexador. Ausentes em versões sem assets copiados.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/zip"
          },
          "default": []
        }
      }
    },
    "shard": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "category",
        "url",
        "assets",
        "bytes"
      ],
      "properties": {
        "category": {
          "type": "string"
        },
        "url": {
          "description": "Caminho da fatia, com hash no nome para permitir cache imutável.",
          "type": "string"
        },
        "assets": {
          "type": "integer",
          "minimum": 0
        },
        "bytes": {
          "type": "integer",
          "minimum": 0
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    },
    "zip": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "category",
        "url",
        "bytes"
      ],
      "properties": {
        "category": {
          "type": "string"
        },
        "url": {
          "type": "string"
        },
        "bytes": {
          "type": "integer",
          "minimum": 0
        },
        "assets": {
          "type": "integer",
          "minimum": 0
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    },
    "catalogRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "url",
        "champions",
        "skins",
        "bytes"
      ],
      "properties": {
        "url": {
          "description": "Caminho do catálogo, com hash no nome para cache imutável.",
          "type": "string"
        },
        "champions": {
          "type": "integer",
          "minimum": 0
        },
        "skins": {
          "type": "integer",
          "minimum": 0
        },
        "bytes": {
          "type": "integer",
          "minimum": 0
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    },
    "generation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "indexer",
        "categories"
      ],
      "properties": {
        "indexer": {
          "description": "Sobe à mão quando um construtor passa a produzir registro diferente.",
          "type": "integer",
          "minimum": 1
        },
        "categories": {
          "description": "As categorias que a execução emite. Categoria nova muda a assinatura.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}
